name = "simple-sieve-generator"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# KV Namespace definitions
# Note: ID is injected via CI/CD
[[kv_namespaces]]
binding = "SIEVE_DATA"
id = "SIEVE_DATA_ID"

[vars]
# Environment variables (BRANCH is set dynamically during deployment)

# Deployment
[triggers]
crons = []

# Demo Environment
[env.demo]
name = "simple-sieve-generator-demo"
vars = { DEMO_MODE = "true" }
